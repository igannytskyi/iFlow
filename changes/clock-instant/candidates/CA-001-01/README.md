Illustrative candidate for the worked example: eleven files under `src/billing`
with `Clock.nowUtc()` rewritten to `Clock.instant()`.

This example is not backed by a real repository; the store exists so that R2 has
something to check and so the shape of a candidate is visible.
