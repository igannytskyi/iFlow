# iFlow

The instructions for working in this repository are in [AGENTS.md](AGENTS.md), which is tool-neutral and is the single source. This file exists only so that Claude Code finds them; it deliberately repeats nothing, because two copies of a rule drift apart.
